任务：基于我刚才生成的 4月19日完整报告，写一个可复用的深度报告生成器。

要求：
1. 读取 templates/deep_report.md（已存在，基于我的报告结构，已去掉第八部分）
2. 创建 plugin/deep_report_final.py
3. 实现 DeepReportFinal 类，包含：
   - _query_stats(start, end)：查数据库，返回所有统计数据
   - _find_best_session()：找目标对齐度最高的session作为"最佳表现"
   - _find_typical_session()：找一个有代表性的session作为"典型案例"
   - _find_weak_session()：找目标对齐度最低的session作为"有待提升"
   - generate(start, end)：组装完整报告，替换模板占位符
4. 关键约束：
   - 日期查询必须用 date(created_at) 兼容时间戳格式
   - 所有SQL必须过滤 is_system_noise=0 和 is_auto_push=0
   - 证据长度>50字才入选"最佳/典型/有待提升"
   - 如果某维度无数据，显示"（无数据）"而非报错
5. 测试：用 4月19日数据生成报告，打印前1000字验证
6. 如果生成结果和我刚才手动写的报告结构一致，就保存文件

不要参考外部指令的代码，按你自己刚才写报告的逻辑来实现。
